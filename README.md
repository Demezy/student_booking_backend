# student_booking_backend

Сервер (прокси) расширяющий функционал api. 
для запуска:
`uvicorn ./app/main:app --reload`

в зависимости от окружения может понадобится убрать префиксы `.` в пионовских 
инпортах
